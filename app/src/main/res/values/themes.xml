<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.MobileUIDesign" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryDark</item>
        <!-- Window background -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Action bar -->
        <item name="actionBarSize">56dp</item>
        <item name="actionBarStyle">@style/Widget.MobileUIDesign.ActionBar</item>
    </style>

    <!-- Action bar style -->
    <style name="Widget.MobileUIDesign.ActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="background">?attr/colorPrimary</item>
        <item name="titleTextAppearance">@style/TextAppearance.MobileUIDesign.ActionBar.Title</item>
    </style>

    <!-- Action bar title style -->
    <style name="TextAppearance.MobileUIDesign.ActionBar.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
    </style>
</resources>